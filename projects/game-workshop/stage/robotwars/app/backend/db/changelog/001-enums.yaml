databaseChangeLog:
  - changeSet:
      id: create-enum-status
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE status_enum AS ENUM ('active', 'disabled', 'suspended', 'deleted', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS status_enum;

  - changeSet:
      id: create-enum-user-status
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE user_status_enum AS ENUM ('active', 'unverified', 'waitlist', 'suspended', 'deleted', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS user_status_enum;

  - changeSet:
      id: create-enum-credential-status
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE credential_status_enum AS ENUM ('active', 'disabled', 'suspended', 'deleted', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS credential_status_enum;

  - changeSet:
      id: create-enum-session-status
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE session_status_enum AS ENUM ('active', 'revoked', 'disabled', 'suspended', 'deleted', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS session_status_enum;

  - changeSet:
      id: create-enum-media-type
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE media_type_enum AS ENUM ('image', 'video', 'audio', 'document', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS media_type_enum;

  - changeSet:
      id: create-enum-file-type
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE file_type_enum AS ENUM ('jpg', 'jpeg', 'png', 'gif', 'webp', 'svg', 'bmp', 'ico', 'tiff', 'mp4', 'avi', 'mov', 'webm', 'mp3', 'wav', 'ogg', 'pdf', 'doc', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS file_type_enum;

  - changeSet:
      id: create-enum-owner-type
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE owner_type_enum AS ENUM ('user', 'organization', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS owner_type_enum;

  - changeSet:
      id: create-enum-credential-type
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE credential_type_enum AS ENUM ('login', 'oauth', 'smart_link', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS credential_type_enum;

  - changeSet:
      id: create-enum-device-type
      author: starter-scaffold
      changes:
        - sql:
            sql: CREATE TYPE device_type_enum AS ENUM ('web', 'ios', 'android', 'other');
      rollback:
        - sql:
            sql: DROP TYPE IF EXISTS device_type_enum;
