apiVersion: v1
kind: Service
metadata:
  name: {{ include "iotgo.fullname" . }}
  labels:
    {{- include "iotgo.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    protocol: TCP
  selector:
    {{- include "iotgo.selectorLabels" . | nindent 4 }}
