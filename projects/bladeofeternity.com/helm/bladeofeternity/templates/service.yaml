apiVersion: v1
kind: Service
metadata:
  name: {{ include "bladeofeternity.fullname" . }}
  labels:
    {{- include "bladeofeternity.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    protocol: TCP
  selector:
    {{- include "bladeofeternity.selectorLabels" . | nindent 4 }}
