apiVersion: v1
kind: Service
metadata:
  name: {{ include "noizu-site.fullname" . }}
  labels:
    {{- include "noizu-site.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    protocol: TCP
  selector:
    {{- include "noizu-site.selectorLabels" . | nindent 4 }}
