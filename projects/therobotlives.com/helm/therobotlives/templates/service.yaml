apiVersion: v1
kind: Service
metadata:
  name: {{ include "therobotlives.fullname" . }}
  labels:
    {{- include "therobotlives.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
    protocol: TCP
  selector:
    {{- include "therobotlives.selectorLabels" . | nindent 4 }}
