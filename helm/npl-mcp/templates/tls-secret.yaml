{{- if and .Values.tls.enabled .Values.tls.infisical.enabled }}
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: {{ .Values.tls.secretName }}
  labels:
    {{- include "npl-mcp.labels" . | nindent 4 }}
spec:
  hostAPI: {{ .Values.tls.infisical.hostAPI }}
  resyncInterval: 300
  authentication:
    universalAuth:
      credentialsRef:
        secretName: {{ .Values.tls.infisical.secretName }}
        secretNamespace: {{ .Values.tls.infisical.credentialsNamespace }}
      secretsScope:
        projectSlug: {{ .Values.tls.infisical.projectSlug }}
        envSlug: {{ .Values.tls.infisical.envSlug }}
        secretsPath: {{ .Values.tls.infisical.secretsPath }}
  managedSecretReference:
    secretName: {{ .Values.tls.secretName }}
    secretNamespace: {{ .Release.Namespace }}
    secretType: kubernetes.io/tls
    creationPolicy: Owner
    template:
      data:
        tls.crt: "{{ `{{ .` }}{{ .Values.tls.infisical.crtKey }}{{ `.Value }}` }}"
        tls.key: "{{ `{{ .` }}{{ .Values.tls.infisical.keyKey }}{{ `.Value }}` }}"
{{- end }}
{{- range (.Values.ingress.extraTls | default list) }}
{{- if and $.Values.tls.enabled .infisical .infisical.enabled }}
---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: {{ .secretName }}
  labels:
    {{- include "npl-mcp.labels" $ | nindent 4 }}
spec:
  hostAPI: {{ $.Values.tls.infisical.hostAPI }}
  resyncInterval: 300
  authentication:
    universalAuth:
      credentialsRef:
        secretName: {{ $.Values.tls.infisical.secretName }}
        secretNamespace: {{ $.Values.tls.infisical.credentialsNamespace }}
      secretsScope:
        projectSlug: {{ $.Values.tls.infisical.projectSlug }}
        envSlug: {{ $.Values.tls.infisical.envSlug }}
        secretsPath: {{ .infisical.secretsPath }}
  managedSecretReference:
    secretName: {{ .secretName }}
    secretNamespace: {{ $.Release.Namespace }}
    secretType: kubernetes.io/tls
    creationPolicy: Owner
    template:
      data:
        tls.crt: "{{ `{{ .` }}{{ .infisical.crtKey }}{{ `.Value }}` }}"
        tls.key: "{{ `{{ .` }}{{ .infisical.keyKey }}{{ `.Value }}` }}"
{{- end }}
{{- end }}
