# Scenario Template
# Copy to ~/.claude/evals/{skill-name}/scenarios/{scenario-name}.yaml

scenario: ""  # kebab-case scenario name
description: ""
difficulty: easy  # easy | medium | hard | adversarial
tags: []

preconditions:
  files_required: []
    # - path: references/some-file.md
    #   must_exist: true
  context: ""

task:
  instruction: ""  # Exact instruction to give the skill
  expected_tools: []  # Tools the skill should use

flow:
  nodes:
    - id: start
      type: trigger
      action: "Skill receives instruction"

    # - id: step-1
    #   type: action
    #   expected_action: "Read reference file X"
    #   expected_tools: [Read]
    #   expected_files: ["references/some-file.md"]
    #   on_success: step-2
    #   on_failure: recover-1

    # - id: step-2
    #   type: terminal
    #   expected_action: "Report completion"
    #   success_criteria:
    #     - "All required files created"

  recovery_paths: []
    # - id: recover-1
    #   trigger: "File not found"
    #   evaluator_action: "Tell skill the file is at alternate path"
    #   expected_skill_response: "Skill adapts and reads from alternate path"
    #   max_retries: 2

  variables: {}
    # skill_path: "~/.claude/skills/{skill-name}"
    # output_dir: "/tmp/eval-output"

success_criteria:
  all_of: []
    # - "All terminal nodes reached"
  any_of: []
    # - "Output matches expected structure"
  none_of: []
    # - "Skill reports error without attempting recovery"

scoring:
  flow_adherence: 0.4
  task_accuracy: 0.3
  recovery_score: 0.2
  efficiency: 0.1
