databaseChangeLog:
  - changeSet:
      id: 016-create-table-policies
      author: starter-scaffold
      changes:
        - sql:
            splitStatements: false
            sql: |
              CREATE TABLE policies (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                name varchar(100) NOT NULL UNIQUE,
                description text,
                policy_document jsonb NOT NULL,
                is_system boolean NOT NULL DEFAULT false,
                is_active boolean NOT NULL DEFAULT true,
                created_by uuid REFERENCES users(id) ON DELETE SET NULL,
                created_at timestamptz DEFAULT now(),
                updated_at timestamptz DEFAULT now()
              );

              CREATE OR REPLACE FUNCTION update_policies_updated_at()
              RETURNS TRIGGER AS $$
              BEGIN
                NEW.updated_at = now();
                RETURN NEW;
              END;
              $$ LANGUAGE plpgsql;

              CREATE TRIGGER update_policies_updated_at
                BEFORE UPDATE ON policies
                FOR EACH ROW
                EXECUTE FUNCTION update_policies_updated_at();

              -- Partial index on name for active policies
              CREATE INDEX idx_policies_name_active
                ON policies (name)
                WHERE is_active = true;

              -- GIN index on policy_document for JSONB queries
              CREATE INDEX idx_policies_document_gin
                ON policies USING GIN (policy_document);
      rollback:
        - sql:
            sql: |
              DROP TRIGGER IF EXISTS update_policies_updated_at ON policies;
              DROP FUNCTION IF EXISTS update_policies_updated_at();
              DROP TABLE policies;

  - changeSet:
      id: 016-create-table-group-policies
      author: starter-scaffold
      changes:
        - sql:
            sql: |
              CREATE TABLE group_policies (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                group_id uuid NOT NULL REFERENCES groups(id) ON DELETE CASCADE,
                policy_id uuid NOT NULL REFERENCES policies(id) ON DELETE CASCADE,
                priority integer NOT NULL DEFAULT 0,
                created_at timestamptz DEFAULT now(),
                CONSTRAINT uq_group_policies_group_policy
                  UNIQUE (group_id, policy_id)
              );

              -- Covering index on group_id
              CREATE INDEX idx_group_policies_group_covering
                ON group_policies (group_id)
                INCLUDE (policy_id, priority);

              -- Index on policy_id
              CREATE INDEX idx_group_policies_policy_id
                ON group_policies (policy_id);
      rollback:
        - dropTable:
            tableName: group_policies

  - changeSet:
      id: 016-create-table-user-policies
      author: starter-scaffold
      changes:
        - sql:
            sql: |
              CREATE TABLE user_policies (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
                policy_id uuid NOT NULL REFERENCES policies(id) ON DELETE CASCADE,
                resource_type resource_type_enum,
                resource_id uuid,
                priority integer NOT NULL DEFAULT 0,
                created_at timestamptz DEFAULT now(),
                CONSTRAINT uq_user_policies_user_policy_resource
                  UNIQUE (user_id, policy_id, resource_type, resource_id)
              );

              -- Covering index on user + resource
              CREATE INDEX idx_user_policies_user_resource_covering
                ON user_policies (user_id, resource_type, resource_id)
                INCLUDE (policy_id, priority);

              -- Index on policy_id
              CREATE INDEX idx_user_policies_policy_id
                ON user_policies (policy_id);
      rollback:
        - dropTable:
            tableName: user_policies
