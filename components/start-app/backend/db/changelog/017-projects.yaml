databaseChangeLog:
  - changeSet:
      id: 017-create-table-projects
      author: starter-scaffold
      changes:
        - sql:
            sql: |
              CREATE TABLE projects (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                organization_id uuid NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
                name varchar(255) NOT NULL,
                slug citext NOT NULL,
                description text,
                settings jsonb DEFAULT '{}',
                status varchar(20) NOT NULL DEFAULT 'active'
                  CHECK (status IN ('active', 'archived', 'deleted')),
                created_by uuid REFERENCES users(id) ON DELETE SET NULL,
                inserted_at timestamptz NOT NULL,
                updated_at timestamptz NOT NULL,
                archived_at timestamptz,
                CONSTRAINT uq_projects_org_slug UNIQUE (organization_id, slug)
              );

              -- Covering index for active projects by org
              CREATE INDEX idx_projects_org_active_covering
                ON projects (organization_id, inserted_at DESC)
                WHERE status = 'active';
      rollback:
        - dropTable:
            tableName: projects
