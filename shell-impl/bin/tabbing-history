#!/usr/bin/env bash
# bin/tabbing-history — search/browse history CLI
source "$(dirname "${BASH_SOURCE[0]}")/_tabbing-wrapper"
_tabbing_check_version "$@"

if [[ $# -eq 0 ]]; then
  _tabbing_history_list_tabs
  exit
fi
_tabbing_history_search "$*"
