# ── CSS Snippets ──
# Optional. Extracted from §05 Buttons, §06 Inputs, §09 Cards.
#
# Custom CSS injected into the generated stylesheet.
# Snippets are ACCUMULATED (merged with base theme, not replaced).
# Use target-section to control where the snippet appears in the guide.

css-snippets:
  # Example: custom button variant
  # - name: "{Snippet Name}"
  #   slug: "{snippet-slug}"
  #   title: "{Display title}"
  #   description: "{What this snippet does}"
  #   target-section: buttons    # or: cards, inputs, ui-elements
  #   body: |
  #     .btn-custom {
  #       background: var(--brand-red);
  #       color: var(--white);
  #       border-radius: var(--radius);
  #       padding: var(--space-2) var(--space-4);
  #     }
  #     .btn-custom:hover {
  #       background: var(--brand-red-mid);
  #     }

  # Example: card variant
  # - name: "Card Glow"
  #   slug: "card-glow"
  #   title: "Glow variant"
  #   description: "Colored glow shadow on hover"
  #   target-section: cards
  #   body: |
  #     .card.glow {
  #       box-shadow: 0 0 20px color-mix(in srgb, var(--brand-blue) 40%, transparent);
  #       transition: box-shadow 0.3s ease;
  #     }
