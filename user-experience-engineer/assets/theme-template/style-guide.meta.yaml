# ── Theme Metadata ──
# Required. Identifies this theme to the engine.
#
# Source: §00 Header & Brand Identity
# The slug becomes the CSS selector: html[data-design-theme="{slug}"]

name: "{Project Name}"
slug: "{project-slug}"
title: "{Project Name} — Style Guide"
description: "{One-line description of the design direction}"
base-theme: "theme-style-guide"
