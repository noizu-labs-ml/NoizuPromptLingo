[project]
name = "noizu-prompt-lingua"
version = "0.1.0"
description = "Noizu Prompt Lingua MCP Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.25.0",
]

[tool.uv.workspace]
members = [
    "tools",
]
