# Provider binaries and cached modules
.terraform/
.terragrunt-cache/

# State files — stored in MinIO, never commit
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
*.pre-split-backup

# Secrets — never commit
*.tfvars
!*.tfvars.example
terraform.tfvars.json

# Plan output files
*.tfplan
tf-plan-logs/
