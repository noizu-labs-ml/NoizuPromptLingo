databaseChangeLog:
  # ── Items domain, consolidated fresh build ─────────────────────────────────
  # Folds trp 026 (items family) + 028 (human keys) + 034 (planning fields) +
  # 036 (personal ownership/tags) into one set of CREATEs with every superset
  # column baked in. npl `tickets`/`ticket_*` → `items`/`item_*`. The single
  # item_number_counters table + its two partial unique indexes are the ON
  # CONFLICT targets for the atomic per-scope key generator.

  - changeSet:
      id: pm-012-item-field-definitions
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: item_field_definitions
      changes:
        - sql:
            sql: |
              CREATE TABLE item_field_definitions (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                organization_id uuid REFERENCES organizations(id) ON DELETE CASCADE,
                project_id uuid REFERENCES projects(id) ON DELETE CASCADE,
                slug varchar(255) NOT NULL,
                label varchar(255) NOT NULL,
                field_type varchar(255) NOT NULL,
                options jsonb,
                default_value varchar(255),
                description varchar(255),
                disabled boolean NOT NULL DEFAULT false,
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE UNIQUE INDEX idx_item_field_definitions_global_slug
                ON item_field_definitions (slug) WHERE organization_id IS NULL AND project_id IS NULL;
              CREATE UNIQUE INDEX idx_item_field_definitions_org_slug
                ON item_field_definitions (organization_id, slug) WHERE organization_id IS NOT NULL AND project_id IS NULL;
              CREATE UNIQUE INDEX idx_item_field_definitions_project_slug
                ON item_field_definitions (organization_id, project_id, slug) WHERE project_id IS NOT NULL;
              CREATE INDEX idx_item_field_definitions_scope ON item_field_definitions (organization_id, project_id);
      rollback:
        - dropTable:
            tableName: item_field_definitions

  - changeSet:
      id: pm-012-item-type-definitions
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: item_type_definitions
      changes:
        - sql:
            sql: |
              CREATE TABLE item_type_definitions (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                organization_id uuid REFERENCES organizations(id) ON DELETE CASCADE,
                project_id uuid REFERENCES projects(id) ON DELETE CASCADE,
                slug varchar(255) NOT NULL,
                name varchar(255) NOT NULL,
                description varchar(255),
                icon varchar(255),
                -- CSS color token for epic (and other) type theming — e.g. "#8b5cf6".
                color varchar(32),
                status_workflow jsonb,
                disabled boolean NOT NULL DEFAULT false,
                deleted_at timestamptz,
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE UNIQUE INDEX idx_item_type_definitions_global_slug
                ON item_type_definitions (slug) WHERE organization_id IS NULL AND project_id IS NULL;
              CREATE UNIQUE INDEX idx_item_type_definitions_org_slug
                ON item_type_definitions (organization_id, slug) WHERE organization_id IS NOT NULL AND project_id IS NULL;
              CREATE UNIQUE INDEX idx_item_type_definitions_project_slug
                ON item_type_definitions (organization_id, project_id, slug) WHERE project_id IS NOT NULL;
              CREATE INDEX idx_item_type_definitions_scope ON item_type_definitions (organization_id, project_id);
              CREATE INDEX idx_item_type_definitions_deleted_at
                ON item_type_definitions (deleted_at) WHERE deleted_at IS NOT NULL;
      rollback:
        - dropTable:
            tableName: item_type_definitions

  - changeSet:
      id: pm-012-item-type-fields
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: item_type_fields
      changes:
        - sql:
            sql: |
              CREATE TABLE item_type_fields (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                item_type_definition_id uuid NOT NULL REFERENCES item_type_definitions(id) ON DELETE CASCADE,
                item_field_definition_id uuid NOT NULL REFERENCES item_field_definitions(id) ON DELETE CASCADE,
                required boolean NOT NULL DEFAULT false,
                position integer NOT NULL DEFAULT 0,
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE INDEX idx_item_type_fields_type_def_id ON item_type_fields (item_type_definition_id);
              CREATE UNIQUE INDEX idx_item_type_fields_unique
                ON item_type_fields (item_type_definition_id, item_field_definition_id);
      rollback:
        - dropTable:
            tableName: item_type_fields

  - changeSet:
      id: pm-012-item-queues
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: item_queues
      changes:
        - sql:
            sql: |
              CREATE TABLE item_queues (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                organization_id uuid REFERENCES organizations(id) ON DELETE CASCADE,
                project_id uuid REFERENCES projects(id) ON DELETE CASCADE,
                name varchar(255) NOT NULL,
                slug varchar(255) NOT NULL,
                description varchar(255),
                methodology varchar(255) NOT NULL DEFAULT 'kanban'
                  CHECK (methodology IN ('kanban', 'scrum', 'waterfall', 'spiral')),
                config jsonb NOT NULL DEFAULT '{}',
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE UNIQUE INDEX idx_item_queues_global_slug
                ON item_queues (slug) WHERE organization_id IS NULL AND project_id IS NULL;
              CREATE UNIQUE INDEX idx_item_queues_org_slug
                ON item_queues (organization_id, slug) WHERE organization_id IS NOT NULL AND project_id IS NULL;
              CREATE UNIQUE INDEX idx_item_queues_project_slug
                ON item_queues (organization_id, project_id, slug) WHERE project_id IS NOT NULL;
              CREATE INDEX idx_item_queues_scope ON item_queues (organization_id, project_id);
      rollback:
        - dropTable:
            tableName: item_queues

  # Wire the deferred projects.default_queue_id FK now that item_queues exists.
  - changeSet:
      id: pm-012-projects-default-queue-fk
      author: noizu-pm
      changes:
        - sql:
            sql: |
              ALTER TABLE projects
                ADD CONSTRAINT fk_projects_default_queue
                FOREIGN KEY (default_queue_id) REFERENCES item_queues(id) ON DELETE SET NULL;
      rollback:
        - sql:
            sql: ALTER TABLE projects DROP CONSTRAINT IF EXISTS fk_projects_default_queue;

  - changeSet:
      id: pm-012-board-stages
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: board_stages
      changes:
        - sql:
            sql: |
              CREATE TABLE board_stages (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                queue_id uuid NOT NULL REFERENCES item_queues(id) ON DELETE CASCADE,
                slug varchar(255) NOT NULL,
                name varchar(255) NOT NULL,
                kind varchar(255) NOT NULL DEFAULT 'stage',
                position integer NOT NULL DEFAULT 0,
                wip_limit integer,
                config jsonb NOT NULL DEFAULT '{}',
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE UNIQUE INDEX idx_board_stages_queue_slug ON board_stages (queue_id, slug);
              CREATE INDEX idx_board_stages_queue_position ON board_stages (queue_id, position);
      rollback:
        - dropTable:
            tableName: board_stages

  - changeSet:
      id: pm-012-board-iterations
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: board_iterations
      changes:
        - sql:
            sql: |
              CREATE TABLE board_iterations (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                queue_id uuid NOT NULL REFERENCES item_queues(id) ON DELETE CASCADE,
                name varchar(255) NOT NULL,
                sequence integer NOT NULL DEFAULT 0,
                status varchar(255) NOT NULL DEFAULT 'planned'
                  CHECK (status IN ('planned', 'active', 'completed')),
                goal text,
                starts_on date,
                ends_on date,
                config jsonb NOT NULL DEFAULT '{}',
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE INDEX idx_board_iterations_queue_seq ON board_iterations (queue_id, sequence);
              CREATE INDEX idx_board_iterations_status ON board_iterations (queue_id, status);
      rollback:
        - dropTable:
            tableName: board_iterations

  # items — full superset (base + planning + personal/tags + human key + lock_version).
  - changeSet:
      id: pm-012-items
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: items
      changes:
        - sql:
            sql: |
              CREATE TABLE items (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                organization_id uuid NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
                project_id uuid REFERENCES projects(id) ON DELETE SET NULL,
                title varchar(255) NOT NULL,
                description text,
                item_type varchar(255) NOT NULL,
                status varchar(255) NOT NULL DEFAULT 'open',
                priority varchar(255),
                assignee varchar(255),
                reporter varchar(255),
                custom_fields jsonb NOT NULL DEFAULT '{}',
                queue_id uuid REFERENCES item_queues(id) ON DELETE SET NULL,
                stage_id uuid REFERENCES board_stages(id) ON DELETE SET NULL,
                iteration_id uuid REFERENCES board_iterations(id) ON DELETE SET NULL,
                parent_id uuid REFERENCES items(id) ON DELETE SET NULL,
                owner_user_id uuid REFERENCES users(id) ON DELETE CASCADE,
                tags text[] NOT NULL DEFAULT '{}',
                rank varchar(255),
                start_date date,
                due_date date,
                estimate numeric(8,2),
                number integer,
                key varchar(64),
                lock_version integer NOT NULL DEFAULT 0,
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE INDEX idx_items_organization_id ON items (organization_id);
              CREATE INDEX idx_items_project_id ON items (project_id);
              CREATE INDEX idx_items_item_type ON items (item_type);
              CREATE INDEX idx_items_status ON items (status);
              CREATE INDEX idx_items_priority ON items (priority);
              CREATE INDEX idx_items_assignee ON items (assignee);
              CREATE INDEX idx_items_queue_id ON items (queue_id);
              CREATE INDEX idx_items_stage_rank ON items (stage_id, rank);
              CREATE INDEX idx_items_due ON items (organization_id, due_date);
              CREATE INDEX idx_items_owner_due
                ON items (organization_id, owner_user_id, due_date)
                WHERE owner_user_id IS NOT NULL;
              CREATE INDEX items_tags_gin ON items USING gin (tags);
      rollback:
        - dropTable:
            tableName: items

  - changeSet:
      id: pm-012-item-links
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: item_links
      changes:
        - sql:
            sql: |
              CREATE TABLE item_links (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                source_item_id uuid NOT NULL REFERENCES items(id) ON DELETE CASCADE,
                target_item_id uuid NOT NULL REFERENCES items(id) ON DELETE CASCADE,
                link_type varchar(255) NOT NULL,
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE INDEX idx_item_links_source ON item_links (source_item_id);
              CREATE INDEX idx_item_links_target ON item_links (target_item_id);
              CREATE UNIQUE INDEX idx_item_links_unique
                ON item_links (source_item_id, target_item_id, link_type);
      rollback:
        - dropTable:
            tableName: item_links

  - changeSet:
      id: pm-012-item-entity-links
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: item_entity_links
      changes:
        - sql:
            sql: |
              CREATE TABLE item_entity_links (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                item_id uuid NOT NULL REFERENCES items(id) ON DELETE CASCADE,
                entity_type varchar(64) NOT NULL,
                entity_id uuid NOT NULL,
                link_type varchar(64) NOT NULL DEFAULT 'relates_to',
                metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE UNIQUE INDEX idx_item_entity_links_uniq
                ON item_entity_links (item_id, entity_type, entity_id, link_type);
              CREATE INDEX idx_item_entity_links_entity
                ON item_entity_links (entity_type, entity_id);
      rollback:
        - dropTable:
            tableName: item_entity_links

  # Per-scope gap-free counter. The two partial unique indexes are the ON
  # CONFLICT targets (idx_inc_org for the NULL-project org bucket, idx_inc_proj
  # for project buckets), collapsing the org bucket to ONE counter per org.
  - changeSet:
      id: pm-012-item-number-counters
      author: noizu-pm
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: item_number_counters
      changes:
        - sql:
            sql: |
              CREATE TABLE item_number_counters (
                id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
                organization_id uuid NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
                project_id uuid REFERENCES projects(id) ON DELETE CASCADE,
                last_number integer NOT NULL DEFAULT 0,
                inserted_at timestamptz NOT NULL DEFAULT now(),
                updated_at timestamptz NOT NULL DEFAULT now()
              );
              CREATE UNIQUE INDEX idx_inc_proj ON item_number_counters (organization_id, project_id) WHERE project_id IS NOT NULL;
              CREATE UNIQUE INDEX idx_inc_org  ON item_number_counters (organization_id)             WHERE project_id IS NULL;
      rollback:
        - dropTable:
            tableName: item_number_counters

  # Human-key number/key uniqueness, per scope. Partial (set only once assigned).
  - changeSet:
      id: pm-012-unique-item-number-proj
      author: noizu-pm
      runInTransaction: false
      changes:
        - sql:
            splitStatements: true
            sql: |
              DROP INDEX IF EXISTS idx_items_proj_number;
              CREATE UNIQUE INDEX CONCURRENTLY idx_items_proj_number
              ON items (organization_id, project_id, number)
              WHERE project_id IS NOT NULL AND number IS NOT NULL;
      rollback:
        - sql:
            sql: DROP INDEX CONCURRENTLY IF EXISTS idx_items_proj_number;

  - changeSet:
      id: pm-012-unique-item-number-org
      author: noizu-pm
      runInTransaction: false
      changes:
        - sql:
            splitStatements: true
            sql: |
              DROP INDEX IF EXISTS idx_items_org_number;
              CREATE UNIQUE INDEX CONCURRENTLY idx_items_org_number
              ON items (organization_id, number)
              WHERE project_id IS NULL AND number IS NOT NULL;
      rollback:
        - sql:
            sql: DROP INDEX CONCURRENTLY IF EXISTS idx_items_org_number;

  - changeSet:
      id: pm-012-unique-item-key
      author: noizu-pm
      runInTransaction: false
      changes:
        - sql:
            splitStatements: true
            sql: |
              DROP INDEX IF EXISTS idx_items_org_key;
              CREATE UNIQUE INDEX CONCURRENTLY idx_items_org_key
              ON items (organization_id, key)
              WHERE key IS NOT NULL;
      rollback:
        - sql:
            sql: DROP INDEX CONCURRENTLY IF EXISTS idx_items_org_key;
