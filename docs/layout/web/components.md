# web/components Directory

No `components/` directory exists in the current `web/` app. Theme component showcases are currently served as standalone HTML files in the root-level theme directories (`blueprint/`, `brush/`, `cyberpunk/`, `sumi-e/`, `swiss/`).
